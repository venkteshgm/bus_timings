{
  "name": "trimet-bus-tracker",
  "version": "1.0.0",
  "description": "Serverless SPA for tracking TriMet bus arrivals",
  "main": "api/bus-times.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "vercel": "^54.18.2"
  }
}
